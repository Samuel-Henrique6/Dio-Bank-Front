import { Container } from "@chakra-ui/react";

const NotFound = () => {
  return (
    <Container maxWidth={"1280px"} flex={"1 0 auto"}>
      NotFound
    </Container>
  );
};

export default NotFound;
